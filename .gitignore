# Dependencies
node_modules/
artifacts/base-sniper/node_modules/
artifacts/api-server/node_modules/

# Build output
artifacts/base-sniper/dist/
artifacts/api-server/dist/

# Database files (local dev DB — don't commit)
artifacts/base-sniper/base.db
artifacts/base-sniper/base.db-shm
artifacts/base-sniper/base.db-wal

# Runtime keys (Telegram + AI keys set via UI — gitignored for security)
# PRIVATE_KEY must ALWAYS be a Replit Secret, never stored in files
artifacts/api-server/.runtime-keys.json

# Env & secrets
.env
.env.local
.env.production

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
